<Window
    x:Class="Clipboard.Windows.Views.LogWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="日志">
    <Grid x:Name="Root" Padding="20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="150" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" FontSize="20" FontWeight="SemiBold" Text="日志" />
            <ComboBox x:Name="LevelComboBox" Grid.Column="1" Header="记录级别" SelectionChanged="LevelComboBox_SelectionChanged">
                <ComboBoxItem Content="跟踪" Tag="trace" />
                <ComboBoxItem Content="调试" Tag="debug" />
                <ComboBoxItem Content="信息" Tag="info" />
                <ComboBoxItem Content="警告" Tag="warn" />
                <ComboBoxItem Content="错误" Tag="error" />
            </ComboBox>
            <ComboBox x:Name="FilterComboBox" Grid.Column="2" Header="筛选级别" SelectionChanged="FilterComboBox_SelectionChanged">
                <ComboBoxItem Content="全部" />
                <ComboBoxItem Content="跟踪" Tag="trace" />
                <ComboBoxItem Content="调试" Tag="debug" />
                <ComboBoxItem Content="信息" Tag="info" />
                <ComboBoxItem Content="警告" Tag="warn" />
                <ComboBoxItem Content="错误" Tag="error" />
            </ComboBox>
        </Grid>
        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchTextBox" PlaceholderText="搜索日志" TextChanged="SearchTextBox_TextChanged" />
            <ToggleSwitch x:Name="AutoRefreshToggle" Grid.Column="1" Margin="0,0,8,0" Header="自动刷新" Toggled="AutoRefreshToggle_Toggled" />
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                <Button Click="RefreshButton_Click" Content="刷新" />
                <Button Click="CopyButton_Click" Content="复制" />
                <Button Click="ClearButton_Click" Content="清空" />
            </StackPanel>
        </Grid>
        <ListView x:Name="LogList" Grid.Row="2" ItemsSource="{x:Bind ViewModel.VisibleLines}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock FontFamily="Consolas" Text="{Binding}" TextWrapping="Wrap" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock x:Name="StorageText" Grid.Row="3" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
    </Grid>
</Window>
